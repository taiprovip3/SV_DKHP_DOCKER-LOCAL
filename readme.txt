0. Config các biến .env từ file .env-example từ các subfolder project.
1. Mở ternimal ngay root folder, chạy lệnh 'docker-compose up -d'.
2. Mở google lên và gõ 'http://localhost:4000/' và trải nghiệm ứng dụng.